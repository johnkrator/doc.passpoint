# API Base URL
# Leave empty to auto-detect (localhost:3000 for dev, Vercel for production)
# Or set explicitly to override auto-detection
VITE_API_URL=

# Site base URL — used for the Ask AI feature to construct the current page URL
# Leave empty in dev (falls back to window.location.origin)
VITE_SITE_URL=
